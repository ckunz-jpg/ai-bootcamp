{
  "name": "vendor-bidding-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Property manager vendor bidding platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "dev:backend": "pnpm --filter backend dev",
    "dev:web": "pnpm --filter web dev",
    "build": "pnpm run --recursive build",
    "lint": "pnpm run --recursive lint"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
